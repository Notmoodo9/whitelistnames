{
  "schemaVersion": 1,
  "id": "whitelistnames",
  "version": "${version}",
  "name": "Whitelist Names",
  "description": "/whitelist add <username> <name> gives players a custom name in chat, tab list and nametag. /changename to rename. Custom whitelist messages.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": ["com.whitelistnames.WhitelistNames"]
  },
  "mixins": ["whitelistnames.mixins.json"],
  "depends": {
    "fabricloader": ">=0.19.3",
    "minecraft": "~26.2",
    "java": ">=25",
    "fabric-api": "*"
  }
}
